{
  "name": "nwhacks-workshop-2024",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "postinstall": "(cd server && npm install) && (cd ../client && npm install)",
    "start": "concurrently \"cd server && npm run start\" \"cd client && npm run start \"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "concurrently": "^7.0.0"
  }
}
